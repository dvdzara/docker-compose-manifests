apiVersion: v1
kind: Service
metadata:
  name: minio-dashboard
spec:
  selector:
    zarantonello.dev/app: minio
    zarantonello.dev/namespace: minio
  ports:
    - port: 80
      targetPort: http-dashboard
